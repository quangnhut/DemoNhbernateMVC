@model PagedList.IPagedList<QuanLyHocSinh.Models.ClassModel>
@using PagedList.Mvc;

@{
    ViewBag.Title = "Class";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h3 style="text-align: center">@ViewBag.Message</h3>
<br />
<form method="get">
    <table>
            <tr>
                <th style="padding-right: 658px">
                    <p>
                        @Html.ActionLink("Create New Department", "Create")
                    </p>
                </th>
                <th style="padding-bottom: 10px">
                    <input type="text" id="name" name="name" />
                    <button class="btn btn-lg btn-success" type="submit" value="Index" style="font-size: x-small">Search</button>
                </th>
            </tr>
    </table>
</form>
<table class="table table-bordered">
    <tr style="background-color: cornflowerblue">
        <th style="text-align: center">
            ID
        </th>
        <th style="text-align: center">
            Phòng ban
        </th>
        <th></th>
        <th></th>
        <th></th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
    <tr>
        <td style="text-align: center">
            @Html.DisplayFor(modelitem => item.ID)
        </td>
        <td style="text-align: center">
            @Html.DisplayFor(modelitem => item.Name)
        </td>

        <td style="text-align: center">
            @Html.ActionLink("Edit", "Edit", new { id = item.ID })
        </td>
        <td style="text-align: center">
            @Html.ActionLink("Delete", "Delete", new { id = item.ID })
        </td>
        <td style="text-align: center">
            @Html.ActionLink("Add New Employee", "InsertStudent", new { id = item.ID })
        </td>
        <td style="text-align: center">
            @Html.ActionLink("View Employee", "ViewStudent", new { id = item.ID })
        </td>
    </tr>
    }
</table>
<div style="margin:30px">
    Trang @(Model.PageCount < Model.PageNumber ? 0 : Model.PageNumber) / @Model.PageCount

    @Html.PagedListPager(Model, page => Url.Action("Index", new { page }))
</div>
